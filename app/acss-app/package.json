{
  "name": "acss-app",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --host 0.0.0.0 & npm run build:css",
    "build:css": "atomizer -o atomic.css index.html -w index.html",
    "build": "tsc && vite build",
    "serve": "vite preview"
  },
  "devDependencies": {
    "typescript": "^4.3.2",
    "vite": "^2.6.4"
  },
  "dependencies": {
    "atomizer": "^3.11.0"
  }
}
